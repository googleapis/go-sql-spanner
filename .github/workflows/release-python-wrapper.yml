name: Build and Publish Python Wrapper

on:
  push:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [ "main" ]
  workflow_dispatch: 
    inputs: 
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'

permissions:
  contents: read

jobs:
  build-spannerlib:
    uses: ./.github/workflows/build-spannerlib-artifacts.yml
